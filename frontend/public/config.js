// 前端运行期动态配置文件 (无需重新打包编译即可覆盖后端服务地址)
window.APP_CONFIG = {
  // 开发调试或同源部署默认走代理 "/api" (由 Nginx 或开发服务器反向代理，彻底消除浏览器跨域与 OPTIONS 预检请求)
  // 若需直连独立跨域后端，可配置为完整 URL，例如: "http://192.168.110.234:8000/api"
  API_BASE_URL: "/api"
};
